import React from 'react';

// Mount your react app into the page
import ReactDOM from 'react-dom';

import StoryBox from './App';

import './index.css';

ReactDOM.render(
  <StoryBox />,
  document.getElementById('root')
);
